apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    {{- include "user-service.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "user-service.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
