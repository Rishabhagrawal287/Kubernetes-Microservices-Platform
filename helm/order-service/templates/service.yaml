apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    {{- include "order-service.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "order-service.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
